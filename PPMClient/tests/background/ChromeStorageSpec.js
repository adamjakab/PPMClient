/**
 * ChromeStorage Tests
 */
define(['TestUtils', 'ChromeStorage'],
    function (TestUtils, ChromeStorage) {

        describe("ChromeStorage Tests", function () {
            /*
            chrome = {};
            chrome.storage = {};
            chrome.storage.local = {};
            chrome.storage.sync = {};
            it("initialize should return Promise", function () {
                TestUtils.isPromise(ChromeStorage.initialize());
            });

            it("shutdown should return Promise", function () {
                TestUtils.isPromise(ChromeStorage.shutdown());
            });
            */
        });
    }
);
